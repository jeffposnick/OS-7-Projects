#include "misc.h"#include "UltraU.h"	void main(void){	float x,y,total,count=0.0L,grandTot=0.0L;	long i,j,k,n,num;	char boo1[2];		Ultra_seed1 = 43761297; Ultra_seed2 = 90872464;	Ultra_Init();		srand(clock());	printf("How many iterations do you want? ");	scanf("%d",&n);	printf("How many times do you want the program to run? ");	scanf("%d",&num);	for(j=0;j<num;j++)	{		for(i=0;i<(n/10);i++)		{			for(k=0;k<10;k++)			{				x=Ultra_vni();				y=Ultra_vni();				if(((x*x)+(y*y))<=1.0)					count++;			}			srand(clock());		}		total=(4.0L*(count/n));		grandTot=grandTot+total;		count=0;	}	printf("\nPi equals %.16Lf",(grandTot/num));	printf("\n\nWould you like to try again? (y/n): ");	scanf("%s",boo1);	if(boo1[0]=='y')	{		CLS;		main();	}}